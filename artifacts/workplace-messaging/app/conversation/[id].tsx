import { Ionicons } from '@expo/vector-icons';
import { router, useLocalSearchParams } from 'expo-router';
import React, { useMemo, useState } from 'react';
import { Alert, FlatList, KeyboardAvoidingView, Modal, Platform, StyleSheet, Text, TextInput, View } from 'react-native';
import { getSenderColor } from '@/constants/senderColors';
import { Avatar } from '@/components/Avatar';
import { Glass } from '@/components/Glass';
import { PressableScale } from '@/components/PressableScale';
import { staff } from '@/data/mockData';
import { useApp } from '@/context/AppContext';
import { useColors } from '@/hooks/useColors';
import { useColorScheme } from 'react-native';

const attachments: Array<{ icon: keyof typeof Ionicons.glyphMap; label: string }> = [
  { icon: 'camera-outline', label: 'Camera' },
  { icon: 'images-outline', label: 'Photo library' },
  { icon: 'document-outline', label: 'Document' },
  { icon: 'bar-chart-outline', label: 'Poll' },
  { icon: 'calendar-outline', label: 'Event' },
];

export default function ConversationScreen() {
  const colors = useColors();
  const isDark = useColorScheme() === 'dark';
  const { id } = useLocalSearchParams<{ id: string }>();
  const { conversations, messages, sendMessage } = useApp();
  const [text, setText] = useState('');
  const [showAttachments, setShowAttachments] = useState(false);
  const [showSearch, setShowSearch] = useState(false);
  const [query, setQuery] = useState('');
  const conversation = conversations.find((item) => item.id === id) ?? conversations[0];
  const items = useMemo(() => (messages[conversation.id] ?? []).filter((item) => item.text.toLowerCase().includes(query.trim().toLowerCase())), [conversation.id, messages, query]);
  const submit = () => { const trimmed = text.trim(); if (!trimmed) return; sendMessage(conversation.id, trimmed); setText(''); };
  const openActions = (body: string) => Alert.alert('Message actions', body, [{ text: 'Reply' }, { text: 'Copy' }, { text: 'Star' }, { text: 'Cancel', style: 'cancel' }]);
  const chooseAttachment = (label: string) => { setShowAttachments(false); Alert.alert(label, `${label} messages are prepared for device integration in the next local build.`); };

  return <KeyboardAvoidingView style={[styles.root, { backgroundColor: colors.background }]} behavior="padding"><View style={[styles.header, { borderBottomColor: colors.border }]}><View style={[styles.signature, { backgroundColor: colors.signatureEnd }]} /><View style={styles.headerRow}><PressableScale onPress={() => router.back()} accessibilityLabel="Go back"><Ionicons name="chevron-back" size={28} color={colors.foreground} /></PressableScale><Avatar label={conversation.avatar} size={40} tone={conversation.kind === 'announcement' ? 'amber' : 'teal'} /><View style={styles.headerCopy}><Text style={[styles.headerTitle, { color: colors.foreground }]}>{conversation.name}</Text><Text style={[styles.headerSub, { color: colors.mutedForeground }]}>{conversation.kind === 'announcement' ? 'Broadcast channel' : conversation.subtitle}</Text></View><PressableScale accessibilityLabel="Search conversation" onPress={() => setShowSearch((value) => !value)}><Ionicons name="search-outline" size={22} color={colors.foreground} /></PressableScale><PressableScale accessibilityLabel="Conversation information" onPress={() => conversation.kind === 'group' ? router.push('/new-chat') : Alert.alert('Conversation info', 'Shared media, profile details, and privacy settings are available here.') }><Ionicons name="ellipsis-horizontal" size={23} color={colors.foreground} /></PressableScale></View>{showSearch ? <View style={[styles.search, { backgroundColor: colors.input, borderColor: colors.border }]}><Ionicons name="search-outline" size={17} color={colors.mutedForeground} /><TextInput value={query} onChangeText={setQuery} autoFocus placeholder="Search messages" placeholderTextColor={colors.mutedForeground} style={[styles.searchInput, { color: colors.foreground }]} /><PressableScale onPress={() => { setQuery(''); setShowSearch(false); }}><Ionicons name="close-circle" size={18} color={colors.mutedForeground} /></PressableScale></View> : null}</View><FlatList data={items} keyExtractor={(item) => item.id} contentContainerStyle={styles.messages} keyboardDismissMode="interactive" keyboardShouldPersistTaps="handled" ListHeaderComponent={<><PressableScale onPress={() => Alert.alert('Earlier messages', 'This local preview keeps the most recent messages on-device.')} style={[styles.loadEarlier, { borderColor: colors.border }]}><Text style={[styles.loadEarlierText, { color: colors.primary }]}>Load earlier messages</Text></PressableScale><View style={styles.dateRow}><View style={[styles.dateRule, { backgroundColor: colors.border }]} /><Text style={[styles.date, { color: colors.mutedForeground }]}>Today</Text><View style={[styles.dateRule, { backgroundColor: colors.border }]} /></View></>} ListEmptyComponent={<Text style={[styles.empty, { color: colors.mutedForeground }]}>No messages match “{query}”.</Text>} renderItem={({ item }) => { const sender = staff.find((person) => person.id === item.senderId); const senderName = item.outgoing ? 'You' : sender?.name ?? 'Team member'; return <View style={[styles.messageRow, item.outgoing && styles.outgoing]}><Text style={[styles.senderName, { color: item.outgoing ? colors.mutedForeground : getSenderColor(item.senderId, isDark) }]}>{senderName}</Text><PressableScale onLongPress={() => openActions(item.text)}><View style={[styles.bubble, item.outgoing ? styles.outgoingBubble : styles.incomingBubble, { backgroundColor: item.outgoing ? colors.royalBlue : colors.bubbleIncoming }]}><Text style={[styles.messageText, { color: item.outgoing ? '#FFFFFF' : colors.messageText }]}>{item.text}</Text><View style={styles.messageMeta}><Text style={[styles.messageTime, { color: item.outgoing ? 'rgba(255,255,255,0.78)' : colors.mutedForeground }]}>{item.time}</Text>{item.outgoing ? <Ionicons name={item.status === 'read' ? 'checkmark-done' : 'checkmark'} size={14} color={item.status === 'read' ? colors.statusDone : 'rgba(255,255,255,0.78)'} /> : null}</View></View></PressableScale></View>; }} /><Glass style={styles.composer}><PressableScale accessibilityLabel="Add attachment" onPress={() => setShowAttachments(true)}><Ionicons name="add-circle-outline" size={27} color={colors.primary} /></PressableScale><TextInput value={text} onChangeText={setText} onSubmitEditing={submit} returnKeyType="send" placeholder={conversation.kind === 'announcement' ? 'Reply to announcement' : 'Write a message'} placeholderTextColor={colors.mutedForeground} style={[styles.textInput, { color: colors.foreground }]} /><PressableScale accessibilityLabel="Send message" onPress={submit} style={[styles.send, { backgroundColor: text.trim() ? colors.royalBlue : colors.input }]}><Ionicons name="arrow-up" size={19} color={text.trim() ? '#FFFFFF' : colors.mutedForeground} /></PressableScale></Glass><Modal transparent visible={showAttachments} animationType="slide" onRequestClose={() => setShowAttachments(false)}><PressableScale style={styles.backdrop} onPress={() => setShowAttachments(false)}><View /></PressableScale><View style={[styles.attachmentSheet, { backgroundColor: colors.card, borderColor: colors.border }]}><View style={[styles.handle, { backgroundColor: colors.border }]} /><Text style={[styles.sheetTitle, { color: colors.foreground }]}>Add to message</Text><View style={styles.attachmentGrid}>{attachments.map((attachment) => <PressableScale key={attachment.label} onPress={() => chooseAttachment(attachment.label)} style={styles.attachment}><View style={[styles.attachmentIcon, { backgroundColor: colors.secondary }]}><Ionicons name={attachment.icon} size={22} color={colors.primary} /></View><Text style={[styles.attachmentLabel, { color: colors.foreground }]}>{attachment.label}</Text></PressableScale>)}</View></View></Modal></KeyboardAvoidingView>;
}

const styles = StyleSheet.create({ root: { flex: 1 }, header: { paddingTop: 46, paddingBottom: 12, paddingHorizontal: 16, borderBottomWidth: 1 }, headerRow: { flexDirection: 'row', alignItems: 'center', gap: 10 }, signature: { height: 3, marginBottom: 10, marginHorizontal: -16 }, headerCopy: { flex: 1 }, headerTitle: { fontFamily: 'Inter_500Medium', fontSize: 17 }, headerSub: { fontFamily: 'Inter_400Regular', fontSize: 12, marginTop: 2 }, search: { minHeight: 40, marginTop: 12, borderWidth: 1, borderRadius: 10, paddingHorizontal: 10, flexDirection: 'row', alignItems: 'center', gap: 7 }, searchInput: { flex: 1, fontFamily: 'Inter_400Regular', fontSize: 15 }, messages: { paddingHorizontal: 16, paddingTop: 10, paddingBottom: 14 }, loadEarlier: { height: 34, alignSelf: 'center', paddingHorizontal: 14, justifyContent: 'center', borderRadius: 17, borderWidth: 1, marginVertical: 10 }, loadEarlierText: { fontFamily: 'Inter_500Medium', fontSize: 13 }, dateRow: { flexDirection: 'row', alignItems: 'center', gap: 10, marginVertical: 12 }, dateRule: { height: 1, flex: 1 }, date: { fontFamily: 'Inter_400Regular', fontSize: 12 }, empty: { textAlign: 'center', fontFamily: 'Inter_400Regular', fontSize: 14, paddingTop: 30 }, messageRow: { alignItems: 'flex-start', marginBottom: 12 }, outgoing: { alignItems: 'flex-end' }, senderName: { fontFamily: 'Inter_500Medium', fontSize: 13, marginBottom: 4, paddingHorizontal: 4 }, bubble: { maxWidth: '82%', paddingHorizontal: 14, paddingTop: 11, paddingBottom: 8, borderRadius: 18 }, incomingBubble: { borderBottomLeftRadius: 4 }, outgoingBubble: { borderBottomRightRadius: 4 }, messageText: { fontFamily: 'Inter_400Regular', fontSize: 17, lineHeight: 25 }, messageMeta: { flexDirection: 'row', justifyContent: 'flex-end', alignItems: 'center', gap: 3, marginTop: 4 }, messageTime: { fontFamily: 'Inter_400Regular', fontSize: 12 }, composer: { marginHorizontal: 14, marginBottom: Platform.OS === 'web' ? 34 : 14, minHeight: 54, borderRadius: 12, flexDirection: 'row', alignItems: 'center', paddingHorizontal: 12, gap: 9 }, textInput: { flex: 1, fontFamily: 'Inter_400Regular', fontSize: 16, maxHeight: 80 }, send: { width: 40, height: 40, borderRadius: 20, alignItems: 'center', justifyContent: 'center' }, backdrop: { ...StyleSheet.absoluteFillObject, backgroundColor: 'rgba(9, 15, 27, 0.36)' }, attachmentSheet: { position: 'absolute', bottom: 0, left: 0, right: 0, borderTopWidth: 1, borderTopLeftRadius: 22, borderTopRightRadius: 22, paddingHorizontal: 20, paddingTop: 10, paddingBottom: Platform.OS === 'web' ? 28 : 38 }, handle: { width: 38, height: 4, borderRadius: 2, alignSelf: 'center' }, sheetTitle: { fontFamily: 'Inter_500Medium', fontSize: 17, marginTop: 16, marginBottom: 18 }, attachmentGrid: { flexDirection: 'row', justifyContent: 'space-between' }, attachment: { alignItems: 'center', width: 58, gap: 7 }, attachmentIcon: { width: 48, height: 48, borderRadius: 24, alignItems: 'center', justifyContent: 'center' }, attachmentLabel: { fontFamily: 'Inter_400Regular', fontSize: 11, textAlign: 'center' } });