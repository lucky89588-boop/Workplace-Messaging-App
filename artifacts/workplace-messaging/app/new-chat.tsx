import { Ionicons } from '@expo/vector-icons';
import { router } from 'expo-router';
import React, { useState } from 'react';
import { ScrollView, StyleSheet, Text, TextInput, View } from 'react-native';
import { Avatar } from '@/components/Avatar';
import { Glass } from '@/components/Glass';
import { PressableScale } from '@/components/PressableScale';
import { Screen } from '@/components/Screen';
import { SectionLabel } from '@/components/SectionLabel';
import { useApp } from '@/context/AppContext';
import { useColors } from '@/hooks/useColors';

const accents = ['#8E1F6B', '#5A35A8', '#0A5C8A', '#0B6E6E', '#8A3F2F'];

export default function NewChatScreen() {
  const colors = useColors();
  const { staff } = useApp();
  const [mode, setMode] = useState<'direct' | 'group'>('direct');
  const [name, setName] = useState('');
  const [houseReference, setHouseReference] = useState('');
  const [description, setDescription] = useState('');
  const [accent, setAccent] = useState(accents[0]);
  const [members, setMembers] = useState<string[]>([]);
  const toggleMember = (id: string) => setMembers((selected) => selected.includes(id) ? selected.filter((member) => member !== id) : [...selected, id]);
  const people = staff.filter((person) => person.id !== 'me');

  return <Screen scroll={false}><View style={styles.top}><PressableScale onPress={() => router.back()} accessibilityLabel="Close"><Ionicons name="close" size={26} color={colors.foreground} /></PressableScale><Text style={[styles.title, { color: colors.foreground }]}>New chat</Text><View style={{ width: 26 }} /></View><View style={[styles.segmented, { backgroundColor: colors.input, borderColor: colors.border }]}>{(['direct', 'group'] as const).map((option) => <PressableScale key={option} onPress={() => setMode(option)} style={[styles.segment, mode === option && { backgroundColor: colors.card }]}><Text style={[styles.segmentText, { color: mode === option ? colors.foreground : colors.mutedForeground }]}>{option === 'direct' ? 'Direct message' : 'Group chat'}</Text></PressableScale>)}</View>{mode === 'direct' ? <ScrollView showsVerticalScrollIndicator={false} contentContainerStyle={styles.scroll}><Text style={[styles.intro, { color: colors.mutedForeground }]}>Start a private conversation with an active staff member.</Text><SectionLabel>PEOPLE</SectionLabel><Glass style={styles.panel}>{people.map((person) => <PressableScale key={person.id} onPress={() => router.push(`/conversation/${person.id === 'alex' ? 'alex-chat' : 'ops'}`)} style={[styles.person, { borderBottomColor: colors.border }]}><Avatar label={person.avatar} size={44} /><View style={styles.copy}><Text style={[styles.name, { color: colors.foreground }]}>{person.name}</Text><Text style={[styles.meta, { color: colors.mutedForeground }]}>{person.title}</Text></View><Ionicons name="chatbubble-ellipses-outline" size={20} color={colors.primary} /></PressableScale>)}</Glass></ScrollView> : <ScrollView showsVerticalScrollIndicator={false} contentContainerStyle={styles.scroll}><Text style={[styles.intro, { color: colors.mutedForeground }]}>Create an operational group for a house, program, or team.</Text><SectionLabel>GROUP DETAILS</SectionLabel><Glass style={styles.form}><Text style={[styles.label, { color: colors.mutedForeground }]}>GROUP NAME</Text><TextInput value={name} onChangeText={setName} placeholder="e.g. Ashfield House team" placeholderTextColor={colors.mutedForeground} style={[styles.input, { color: colors.foreground, borderColor: colors.border }]} /><Text style={[styles.label, { color: colors.mutedForeground }]}>HOUSE REFERENCE</Text><TextInput value={houseReference} onChangeText={setHouseReference} placeholder="e.g. ASH-01" placeholderTextColor={colors.mutedForeground} style={[styles.input, { color: colors.foreground, borderColor: colors.border }]} /><Text style={[styles.label, { color: colors.mutedForeground }]}>DESCRIPTION</Text><TextInput value={description} onChangeText={setDescription} placeholder="What should this group be used for?" placeholderTextColor={colors.mutedForeground} multiline style={[styles.input, styles.textArea, { color: colors.foreground, borderColor: colors.border }]} /><Text style={[styles.label, { color: colors.mutedForeground }]}>ACCENT COLOUR</Text><View style={styles.swatches}>{accents.map((value) => <PressableScale key={value} onPress={() => setAccent(value)} style={[styles.swatch, { backgroundColor: value, borderColor: accent === value ? colors.foreground : 'transparent' }]}>{accent === value ? <Ionicons name="checkmark" size={16} color="#fff" /> : null}</PressableScale>)}</View></Glass><SectionLabel>MEMBERS · {members.length}</SectionLabel><Glass style={styles.panel}>{people.map((person) => <PressableScale key={person.id} onPress={() => toggleMember(person.id)} style={[styles.person, { borderBottomColor: colors.border }]}><Avatar label={person.avatar} size={42} /><View style={styles.copy}><Text style={[styles.name, { color: colors.foreground }]}>{person.name}</Text><Text style={[styles.meta, { color: colors.mutedForeground }]}>{person.department}</Text></View><Ionicons name={members.includes(person.id) ? 'checkmark-circle' : 'ellipse-outline'} size={23} color={members.includes(person.id) ? colors.primary : colors.mutedForeground} /></PressableScale>)}</Glass><PressableScale onPress={() => router.push('/conversation/ops')} style={[styles.create, { backgroundColor: colors.royalBlue }]}><Text style={styles.createText}>Create group chat</Text></PressableScale></ScrollView>}</Screen>;
}

const styles = StyleSheet.create({ top: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', marginBottom: 18 }, title: { fontFamily: 'Inter_500Medium', fontSize: 18 }, segmented: { height: 46, borderRadius: 12, borderWidth: 1, padding: 3, flexDirection: 'row', marginBottom: 8 }, segment: { flex: 1, borderRadius: 9, justifyContent: 'center', alignItems: 'center' }, segmentText: { fontFamily: 'Inter_500Medium', fontSize: 14 }, scroll: { paddingBottom: 36 }, intro: { fontFamily: 'Inter_400Regular', fontSize: 16, lineHeight: 23, marginVertical: 16 }, panel: { paddingHorizontal: 15, borderRadius: 12 }, person: { minHeight: 70, flexDirection: 'row', alignItems: 'center', gap: 13, borderBottomWidth: 1 }, copy: { flex: 1, gap: 4 }, name: { fontFamily: 'Inter_500Medium', fontSize: 16 }, meta: { fontFamily: 'Inter_400Regular', fontSize: 13 }, form: { padding: 16, borderRadius: 12, gap: 8 }, label: { fontFamily: 'Inter_500Medium', fontSize: 11, letterSpacing: .7, marginTop: 3 }, input: { minHeight: 46, borderWidth: 1, borderRadius: 10, paddingHorizontal: 12, fontFamily: 'Inter_400Regular', fontSize: 15 }, textArea: { minHeight: 82, paddingTop: 12, textAlignVertical: 'top' }, swatches: { flexDirection: 'row', gap: 11, paddingVertical: 3 }, swatch: { width: 32, height: 32, borderRadius: 16, borderWidth: 2, alignItems: 'center', justifyContent: 'center' }, create: { height: 52, justifyContent: 'center', alignItems: 'center', borderRadius: 26, marginTop: 20 }, createText: { color: '#fff', fontFamily: 'Inter_500Medium', fontSize: 16 } });